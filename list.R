#list

name <- c("Beckham","Ronaldo","meaai")
age <- c(35,32,30)
clubs <- c("Manl","Madrrid","Barca")
refired <- c(T,F,F)

my_list1 <- list(name,age,clubs,refired)
my_list2 <- list(player_name = name,
                 player_age = age,
                 player_clubs = clubs,
                 player_refired = refired)
